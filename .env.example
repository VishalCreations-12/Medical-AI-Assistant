JWT_SECRET=replace-with-a-long-random-string-for-production
PORT=8080
ENV=development
CORS_ORIGINS=http://localhost:3000
# Optional: point the browser at an external Go API instead of the built-in in-memory API.
# NEXT_PUBLIC_API_URL=http://localhost:8080
REDIS_ADDR=
REDIS_PASSWORD=
DATABASE_URL=
